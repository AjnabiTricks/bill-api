{
  "name": "bill-api",
  "version": "2.0.0",
  "description": "Multi-search API for electricity bills",
  "main": "api/search.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "^35.0.0"
  }
}
